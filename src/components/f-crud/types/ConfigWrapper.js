/**
 *
 * @author zzhijie
 * @date 2021-04-25
 */

export class ConfigWrapper{
  /**
   *
   * @param config {Config}
   * @returns {Config}
   */
  constructor(config) {
    return config;
  }
  /**
   * 获取配置项
   * @param config {Config}
   * @returns {Config}
   */
  static convert(config) {
    return config;
  }
}
